{
  "name": "communitymap",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npm run dev:clean && concurrently -k -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --hostname 127.0.0.1 --port 3000\"",
    "dev:clean": "node scripts/clean-dev-ports.cjs",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "stack:up": "run.bat",
    "seed:demo": "npm run seed:demo --prefix backend -- append",
    "seed:demo:sync": "npm run seed:demo --prefix backend -- sync",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "dotenv": "^16.5.0"
  }
}
